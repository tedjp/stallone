# Configuration file for the Stallone NAT-PMP gateway daemon
# See stallone(8) for a description of the options.
# This file follows a strict ini-like syntax.

[natpmd]
  min-port=@NATPMD_DEFAULT_MIN_PORT@
  max-port=@NATPMD_DEFAULT_MAX_PORT@
  mapping-script=@NATPMD_DEFAULT_MAPPING_SCRIPT@
